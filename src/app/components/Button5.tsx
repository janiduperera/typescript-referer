// Interface is similar type props but it has to be an object
interface IButtonProps {
  backgroundColor: string;
  fontSize: number;
}

export default function Button5({ backgroundColor, fontSize }: IButtonProps) {
  return <div></div>;
}
